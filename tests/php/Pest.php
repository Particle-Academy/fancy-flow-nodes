<?php

declare(strict_types=1);

// Plain PHP tests — the executors are framework-free, so nothing here needs a
// booted application. The Laravel service provider is the only Laravel-aware
// piece, and it is exercised by consumers rather than mocked here.
